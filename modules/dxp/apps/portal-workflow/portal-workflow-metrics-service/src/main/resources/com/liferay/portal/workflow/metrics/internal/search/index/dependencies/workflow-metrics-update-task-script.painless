if (ctx._source.tasks != null) {
	def task = ctx._source.tasks.find(task -> task.taskId == params.taskId);

	if (task != null) {
		task.assigneeIds = params.assigneeIds;
		task.assigneeType = params.assigneeType;
	}
}